package br.edu.ifpb.restdelivery.dao;



public class Teste {
	public static void main(String[] args) {

		/*EntityManagerFactory emf = null;
		EntityManager em = null;
		EntityTransaction tx = null;
	//	EmployeeService userService = null;

		try {
			emf = Persistence.createEntityManagerFactory("post");
			em = emf.createEntityManager();
			userService = new EmployeeService(new ImplEmployeeDAO());

			tx = em.getTransaction();
			tx.begin();
			Employee emp = new Employee();
			Address ad = new Address();
			ad.setCity("Humaitá");
			ad.setState("Amazonas");
			ad.setNeighborhood("Centro");
			ad.setNumber("34");
			ad.setZipCode("68400-000");
		
			emp.setAddress(ad);
			emp.setCpf("939.237.514-00");
			emp.setCtps("233293209302");
			emp.setRg("233i428493283");
			emp.setSexo('M');
			
			emp.setName("Rafael");
			emp.setUser(new User());
			emp.getUser().setEmail("jose@gmail.com");
			emp.getUser().setLogin("admin3");
			emp.getUser().setPassword(Tools.criptografarSenha("123"));
			emp.getUser().setPerson(emp);
			
			//userService.saveEmployee(emp);

			
			tx.commit();
		} catch (PersistenceException pe) {
			pe.printStackTrace();
			if (tx != null) {
				tx.rollback();
			}
		} catch (RestDeliveryException e) {
			e.printStackTrace();
			if (tx != null) {
				tx.rollback();
			}
		} finally {
			if (em != null) {
				em.close();
			}
			if (emf != null) {
				emf.close();
			}
*/		}

	

	

}
